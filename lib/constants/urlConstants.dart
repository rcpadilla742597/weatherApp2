import 'package:html/parser.dart';

const String baseUrl = 'https://www.nytimes.com';
const String queryUrl = '$baseUrl/search?query=';


// get HTML document
// get Request and parse it var d = parse(response.body);
// get the response body - Element? querySelector(String selector) =>
    // query.querySelector(this, selector);
    //print static orlando 
// parse it
// document.querySelector("#site-content > div > div:nth-child(2) > div.css-46b038 > ol")

//go to the api reference

//do i organize this as a class, or global function. test this out in main.dart.
//before the run app.
